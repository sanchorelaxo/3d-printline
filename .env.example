# 3d-printline Pipeline Configuration
# Copy to pipeline/.env and fill in your values.

# OpenScanCloud
OSC_TOKEN=your_openscancloud_token
OSC_SERVER=http://openscanfeedback.dnsuser.de:1334/
OSC_USER=openscan
OSC_PASS=free

# OpenScan Mini (Samba)
OPENSCAN_HOST=openscan.local
OPENSCAN_SMB_USER=pi
OPENSCAN_SMB_PASS=raspberry
OPENSCAN_SCAN_PATH=OpenScan/scans

# Bambu X1-Carbon
BAMBU_SERIAL=your_printer_serial
BAMBU_ACCESS_CODE=your_access_code
BAMBU_MQTT_USER=bblp
BAMBU_MQTT_PORT=8883
BAMBU_FTP_PORT=990

# Laptop (orchestrator — runs Blender/Slicer/Print locally)
LAPTOP_PIPELINE_DIR=/path/to/3d-printline
BAMBU_STUDIO_DIR=/path/to/appImages

# Pi (laptop SSHes into Pi for steps 0-2 + notifications)
PI_HOST=192.168.1.x
PI_USER=piuser
PI_HOME=/home/piuser
PI_PIPELINE_DIR=/home/piuser/3d-printline/pipeline
OPENCLAW_BIN=/home/piuser/.npm-global/bin/openclaw

# Pipeline defaults
DECIMATE_RATIO=0.5
SLICER_PROFILE=0.16mm High Quality @BBL X1C
FILAMENT=Generic PLA
SCAN_POLL_INTERVAL=60
CLOUD_POLL_INTERVAL=60

# Telegram notifications (openclaw message send --target)
# Set to your Telegram chat ID or @username, leave empty to skip notifications
TELEGRAM_TARGET=

# Local paths (on Pi)
SCANDATA_DIR=/mnt/scandata
